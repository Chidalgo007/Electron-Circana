mkdir my-electron-app
cd my-electron-app
npm init -y
# Electron
npm install --save-dev electron

# Puppeteer (browser automation)
npm install playwright-core

# for windows Credential vault
npm install keytar

# for electron store vault
npm install electron-store keytar

# Child process utils (built-in with Node, but cross-spawn is safer on Windows)
npm install cross-spawn

# setup package.JSON
"main": "main.js",
"scripts": {
  "start": "electron ."

# insatall electron builder
npm install --save-dev electron-builder

# to built ini
npx electron-builder --win
or
npm run dist

# for any python script run as standlone 
pyinstaller --onefile --noconsole your_script.py
